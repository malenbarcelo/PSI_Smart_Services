export interface BaseFileParams {
    rotate?: 0 | 90 | 180 | 270;
    password?: string;
}
export default class BaseFile {
    taskId: string;
    serverFilename: string;
    filename: string;
    params: BaseFileParams;
    constructor(taskId?: string, serverFilename?: string, filename?: string, params?: BaseFileParams);
}
